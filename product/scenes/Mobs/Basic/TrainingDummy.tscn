[gd_scene load_steps=7 format=3]

[ext_resource type="SpriteFrames" path="res://sprites/monsters/skeleton_warrior.aseprite" id="1"]
[ext_resource type="PackedScene" path="res://scenes/Mobs/Mob.tscn" id="2"]

[sub_resource type="CircleShape2D" id=1]
radius = 4.0

[sub_resource type="CircleShape2D" id=2]
radius = 4.0

[sub_resource type="Shader" id=3]
code = "shader_type canvas_item;

uniform float speed = 1.;

uniform vec4 tint : source_color = vec4(1., 1., 0., 1.);

uniform float span : hint_range(0.1, 1.) = .3;

uniform bool enabled = true;

float luminance(vec4 colour) {
	return 1.0 - sqrt(0.299*colour.r*colour.r + 0.587*colour.g*colour.g + 0.114*colour.b*colour.b);
}

void fragment() {
	vec4 colour = texture(TEXTURE, UV);
    if (enabled) {
       	float target = abs(sin(TIME * PI * speed) * (1. + span));
       	if(colour.a > 0.) {
      		float lum = luminance(colour);
      		float diff = abs(lum - target);
      		float mx = clamp(1. - diff / span, 0., 1.);
      		colour = mix(colour, tint, mx);
       	}
    }
	
	COLOR = colour;
}"

[sub_resource type="ShaderMaterial" id=4]
resource_local_to_scene = true
shader = SubResource("3")
shader_parameter/speed = 1.0
shader_parameter/tint = Color(1, 0, 0, 1)
shader_parameter/span = 0.3
shader_parameter/enabled = false

[node name="TrainingDummy" instance=ExtResource("2")]
collision_layer = 32
collision_mask = 288
mode = 1
type="TRAINING_DUMMY"
does_melee_attack = false

[node name="CollisionShape2D" parent="." index="0"]
shape = SubResource("1")

[node name="CollisionShape2D" parent="MobCollider" index="0"]
shape = SubResource("2")

[node name="Sprite" parent="SpriteContainer" index="0"]
material = SubResource("4")
frames = ExtResource("1")
frame = 0

[node name="Stats" parent="." index="4"]
override_base_health_max = true
base_health_max_override = 1e+08
track_dps = true
