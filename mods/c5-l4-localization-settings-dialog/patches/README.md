# C5-L4 settings dialog patch

This manifest-localized slice contains only the three static `DISPLAY_SAFE`
navigation labels in `Settings.tscn`:

- `Settings` -> `设置`
- `Close Settings` -> `关闭设置`
- `Keybindings` -> `按键设置`

The source preimage is guarded by the pristine `03_raw` file hash. No script,
node name, signal, path, or other settings control is changed.
