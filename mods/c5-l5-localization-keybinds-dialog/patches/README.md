# C5-L5 keybinds dialog patch

This manifest-localized slice contains only the two static `DISPLAY_SAFE`
`text` fields in `Keybinds.tscn`:

- `Keyboard Settings` -> `键盘设置`
- `Done` -> `完成`

The source preimage is guarded by the pristine `03_raw` file hash. Input
action names, `label_text` values, node names, signals, paths, and scripts are
not changed by this batch.
