# C5-L2 character-selection slice

This manifest is a controlled Phase 6 expansion from the trusted C5-L1 menu
milestone. It contains four exact `DISPLAY_SAFE` text units: the existing
`开始游戏` control plus three labels in `CharacterSelect.tscn`.

The source tree is never edited in place. A fresh generated worktree must be
derived from `04_recovered`, and the production pack must be copied from
`03_raw` before these declared deltas are applied.
